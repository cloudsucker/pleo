<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Личный кабинет</title>
        <style>
            body {
                margin: 0;
                height: 100vh;
                display: flex;
                flex-direction: column;
                background: #121212;
                font-family: "Segoe UI", sans-serif;
                color: #e0e0e0;
            }
            header {
                display: flex;
                justify-content: flex-end;
                align-items: center;
                padding: 15px 20px;
                background: #1e1e1e;
                border-bottom: 1px solid #333;
            }
            header .username {
                margin-right: 20px;
                font-size: 14px;
                color: #bbb;
            }
            header button {
                padding: 8px 14px;
                border: none;
                border-radius: 6px;
                background: #4f46e5;
                color: white;
                font-size: 14px;
                cursor: pointer;
                transition: 0.2s;
            }
            header button:hover {
                background: #3730a3;
            }
            main {
                flex: 1;
                display: flex;
                justify-content: center;
                align-items: center;
            }
            .card {
                background: #1e1e1e;
                padding: 30px;
                border-radius: 12px;
                width: 400px;
                text-align: center;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
            }
            h1 {
                margin-bottom: 20px;
                font-size: 22px;
            }
        </style>
    </head>
    <body>
        <header>
            <span class="username">👤 {{ user.username }}</span>
            <button id="logoutBtn">Logout</button>
        </header>

        <main>
            <div class="card">
                <h1>Добро пожаловать, {{ user.username }}!</h1>
                <p>Это ваш личный кабинет.</p>
                <h3>Ваша почта: {{ user.email or "Не указана" }}</h3>
                <h3>Вы с нами с: {{ user.created_at }}</h3>
            </div>
        </main>

        <script>
        document.getElementById("logoutBtn").addEventListener("click", async () => {
            await fetch("/auth/logout", { method: "POST", credentials: "include" });
            window.location.href = "/auth/login";
        });
        </script>
    </body>
</html>
